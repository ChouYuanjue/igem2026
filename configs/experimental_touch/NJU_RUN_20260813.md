# NJU ExperimentalTouch cascade run — 2026-08-13

Profile: `profiles/nju_lab_gbk_20260812.yaml`
Run ID: `bootstrap_v2_20260813`
Protocol: `experimental-touch-cascade-v1-20260813`

This is a non-sequence summary of the isolated server-side run. Private candidate sequences and local SQLite databases are not committed.

## Stage cascade

| Stage | Input / promoted | Finalized at stage | Purpose |
|---|---:|---:|---|
| Candidate universe | 131,532 | — | isolated NJU TPS-admissible library |
| Stage 1 | 131,532 | 128,375 | cheap exact-sequence/public-registry pass |
| Stage 2 | 3,157 | 2,039 | rich UniProt evidence + experimental PDB verification |
| Stage 3 | 1,118 | 1,118 | targeted linked/alias literature evidence |

Stage 2 therefore touched 2.40% of the library; Stage 3 touched 0.85%.

The model-priority guardrails were Top 200 per unique reaction group for Stage 2 (2,859 unique candidates) and Top 50 per group for Stage 3 (773 unique candidates). Evidence signals can additionally promote a candidate, so the actual Stage 2 and Stage 3 queues are larger than the forced-focus sets.

## Final T0–T5 counts

| Touch level | Count |
|---|---:|
| T0 | 31,171 |
| T1 | 100,324 |
| T2 | 1 |
| T3 | 7 |
| T4 | 8 |
| T5 | 21 |

Strict T2–T5 positive-evidence pool: **37**.

Stage 3 changed 12 tags relative to the validated v2 snapshot. Eleven were deeper upgrades within the existing T2+ pool; one candidate moved from T1 to T5. No candidate was left in `REVIEW` after the frozen Stage 3 rules.

## Isolation

Candidate source is read-only. Reusable public evidence is stored separately from candidate/run state:

- evidence role: `local_candidate_libraries/experimental_touch_evidence/uniprot_2026_02/evidence.sqlite`
- run-state role: `local_candidate_libraries/experimental_touch_runs/nju_lab_gbk_20260812/bootstrap_v2_20260813/run.sqlite`

The runtime role check prevents those SQLite databases from being interchanged. Allowed-root checks prevent runtime databases from being placed in the production/upstream database repository.

The production/upstream database was not modified by this run.

## Server-only exports

The ignored private run directory contains:

- `exports/final_tags.tsv.gz`
- `exports/stage3_upgrades.csv`
- `exports/run_manifest.json`

These exports are intentionally not committed because they are candidate-library/run artifacts rather than reusable source code.
