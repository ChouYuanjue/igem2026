project:
  name: demo_p2rank_topk
  run_id: demo_p2rank_topk
  seed: 42

external:
  enzymecage_root: external_repos/EnzymeCAGE
  p2rank_home: external_repos/EnzymeCAGE/tools/p2rank_2.5.1

data:
  reaction_csv: external_repos/EnzymeCAGE/dataset/demo/reaction.csv
  structure_dir: external_repos/EnzymeCAGE/dataset/demo/structures
  working_dir: data/pocket_runs/demo_p2rank_topk
  output_dir: results/pocket/demo_p2rank_topk

model:
  checkpoint_dir: external_repos/EnzymeCAGE/checkpoints/pretrain/seed_42
  checkpoint_name: epoch_19.pth

pocket:
  method: p2rank_topk
  source: p2rank
  selection: topk
  union_sources:
    - p2rank
  top_k: 5

aggregation:
  method: softmax_pool
  temperature: 0.2

evaluation:
  label_csv: null
  topk:
    - 1
    - 3
    - 5
    - 10
