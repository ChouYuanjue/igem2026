# Projects

Project code is split by research direction.

- `active/`: runnable projects with code, configs, and scripts.

Use `projects/active/<project>/README.md` as the first stop for each active
workflow.

Local parked/planned directions may exist in a developer workspace, but they are not
part of the scientific release index until they become runnable research artifacts.
