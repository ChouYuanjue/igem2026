"""Application-only FIBRE specializations.

Nothing in this namespace is part of the portable label-free method contract.
Application assets may use all accepted project data and pair-supervised
specialization, but must never be imported by frozen benchmark reproduction.
"""
