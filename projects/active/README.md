# Active Projects

Active projects contain runnable source that is part of the current research/production line.

- `terpene_screening/`: terpene synthase screening, retrieval models, and wet-lab candidate gates.

Retired local experiments such as the former pocket-robustness branch are intentionally excluded by the repository `.gitignore`.
